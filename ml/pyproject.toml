[project]
name = "nextroute-ml"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "polars",
    "connectorx",
    "pyarrow",
    "python-dotenv",
    "lightgbm",
    "scikit-learn",
    "numpy",
    "pandas",
    "psycopg[binary]",
]

[tool.uv]
package = false
