# NextRoute MVP 앱 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 공공데이터 API 운영계정 전환 심사용 MVP 배포 — 경로검색·즐겨찾기·실시간 도착정보 제공

**Architecture:** Spring Boot(헥사고날 Ports & Adapters) REST API + Expo RN 앱. 백엔드에 도착정보 API 2개와 즐겨찾기 CRUD를 추가하고, 신규 Expo 프로젝트(nextroute-app/)에서 소비한다. 인증은 `X-Device-Id` 헤더 기반 익명 User로 처리하며 Spring Security는 추후 도입한다.

**Tech Stack:** Spring Boot 3.5.11 / Java 17 / JPA / PostgreSQL | Expo SDK 51 / Expo Router v3 / TypeScript / axios

---

## 파일 맵

### Spring Boot (nextroute/)

| 상태 | 파일 | 역할 |
|------|------|------|
| 수정 | `domain/user/entity/User.java` | deviceId 컬럼 추가 |
| 신규 | `domain/user/repository/UserRepository.java` | findByDeviceId |
| 신규 | `domain/user/service/UserDomainService.java` | findOrCreate(deviceId) |
| 수정 | `domain/bus/repository/BusArrivalRawRepository.java` | stopId 기반 native query |
| 수정 | `domain/bus/service/BusDataService.java` | findLatestArrivalsByStopId |
| 수정 | `domain/subway/repository/SubwayArrivalRawRepository.java` | stationId + collectedAt 조회 |
| 수정 | `domain/subway/service/SubwayDataService.java` | findLatestArrivalsByStationId |
| 수정 | `domain/route/favorite/entity/FavoriteRoute.java` | 필드명 정리 + Builder |
| 수정 | `domain/route/favorite/repository/FavoriteRouteRepository.java` | Long PK 수정 + 쿼리 추가 |
| 수정 | `domain/route/favorite/service/FavoriteRouteService.java` | CRUD 메서드 추가 |
| 신규 | `application/arrival/port/in/GetBusArrivalUseCase.java` | Port In |
| 신규 | `application/arrival/port/in/GetSubwayArrivalUseCase.java` | Port In |
| 신규 | `application/arrival/dto/BusArrivalResponse.java` | 응답 DTO |
| 신규 | `application/arrival/dto/SubwayArrivalResponse.java` | 응답 DTO |
| 신규 | `application/arrival/service/BusArrivalQueryService.java` | UseCase 구현 |
| 신규 | `application/arrival/service/SubwayArrivalQueryService.java` | UseCase 구현 |
| 수정 | `application/route/port/in/AddFavoriteRouteUseCase.java` | 시그니처 완성 |
| 신규 | `application/route/port/in/GetFavoriteRoutesUseCase.java` | Port In |
| 신규 | `application/route/port/in/DeleteFavoriteRouteUseCase.java` | Port In |
| 신규 | `application/route/dto/FavoriteRequest.java` | 요청 DTO |
| 신규 | `application/route/dto/FavoriteResponse.java` | 응답 DTO |
| 수정 | `application/route/service/FavoriteRouteService.java` | UseCase 3개 구현 |
| 신규 | `infrastructure/adapter/in/api/arrival/BusArrivalController.java` | GET /api/arrivals/bus/{stopId} |
| 신규 | `infrastructure/adapter/in/api/arrival/SubwayArrivalController.java` | GET /api/arrivals/subway/{stationId} |
| 수정 | `infrastructure/adapter/in/api/route/favorite/FavoriteController.java` | POST/GET/DELETE 완성 |
| 신규 | `infrastructure/config/CorsConfig.java` | Expo 웹 CORS 허용 |

### Expo RN (nextroute-app/ — 신규 프로젝트)

| 파일 | 역할 |
|------|------|
| `constants/Config.ts` | API_BASE_URL, KAKAO_API_KEY |
| `lib/api.ts` | axios 인스턴스 + deviceId 헤더 자동 삽입 |
| `lib/storage.ts` | deviceId AsyncStorage 관리 |
| `app/(tabs)/index.tsx` | 경로검색 탭 |
| `app/(tabs)/favorites.tsx` | 즐겨찾기 탭 |
| `app/route-result.tsx` | 경로 결과 스택 화면 |
| `app/arrivals/bus/[stopId].tsx` | 버스 도착정보 |
| `app/arrivals/subway/[stationId].tsx` | 지하철 도착정보 |
| `components/RouteCard.tsx` | 경로 결과 카드 |
| `components/ArrivalList.tsx` | 도착정보 리스트 |

---

## Task 1: User 엔티티 + UserRepository + UserDomainService

**Files:**
- Modify: `src/main/java/watoo/grd/nextroute/domain/user/entity/User.java`
- Create: `src/main/java/watoo/grd/nextroute/domain/user/repository/UserRepository.java`
- Create: `src/main/java/watoo/grd/nextroute/domain/user/service/UserDomainService.java`
- Test: `src/test/java/watoo/grd/nextroute/domain/user/service/UserDomainServiceTest.java`

- [ ] **Step 1: User.java에 deviceId 추가**

```java
// src/main/java/watoo/grd/nextroute/domain/user/entity/User.java
package watoo.grd.nextroute.domain.user.entity;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Table;
import lombok.AccessLevel;
import lombok.Builder;
import lombok.Getter;
import lombok.NoArgsConstructor;
import watoo.grd.nextroute.common.entity.BaseEntity;

@Entity
@Table(name = "users")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
public class User extends BaseEntity {

    @Column(unique = true, nullable = false)
    private String deviceId;

    @Builder
    public User(String deviceId) {
        this.deviceId = deviceId;
    }
}
```

> 주의: `@Table(name = "users")` — 기본 `user`는 PostgreSQL 예약어라 오류남.

- [ ] **Step 2: UserRepository 생성**

```java
// src/main/java/watoo/grd/nextroute/domain/user/repository/UserRepository.java
package watoo.grd.nextroute.domain.user.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import watoo.grd.nextroute.domain.user.entity.User;

import java.util.Optional;

public interface UserRepository extends JpaRepository<User, Long> {
    Optional<User> findByDeviceId(String deviceId);
}
```

- [ ] **Step 3: UserDomainService 테스트 작성 (실패 확인)**

```java
// src/test/java/watoo/grd/nextroute/domain/user/service/UserDomainServiceTest.java
package watoo.grd.nextroute.domain.user.service;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import watoo.grd.nextroute.domain.user.entity.User;
import watoo.grd.nextroute.domain.user.repository.UserRepository;

import java.util.Optional;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.BDDMockito.given;
import static org.mockito.Mockito.never;
import static org.mockito.Mockito.verify;

@ExtendWith(MockitoExtension.class)
class UserDomainServiceTest {

    @Mock UserRepository userRepository;
    @InjectMocks UserDomainService userDomainService;

    @Test
    void findOrCreate_existingUser_returnsWithoutSave() {
        User existing = User.builder().deviceId("device-1").build();
        given(userRepository.findByDeviceId("device-1")).willReturn(Optional.of(existing));

        User result = userDomainService.findOrCreate("device-1");

        assertThat(result.getDeviceId()).isEqualTo("device-1");
        verify(userRepository, never()).save(any());
    }

    @Test
    void findOrCreate_newDevice_savesAndReturns() {
        User saved = User.builder().deviceId("new-device").build();
        given(userRepository.findByDeviceId("new-device")).willReturn(Optional.empty());
        given(userRepository.save(any())).willReturn(saved);

        User result = userDomainService.findOrCreate("new-device");

        assertThat(result.getDeviceId()).isEqualTo("new-device");
        verify(userRepository).save(any());
    }
}
```

- [ ] **Step 4: 테스트 실행 — FAIL 확인**

```bash
cd /Users/wa/golbob/개인/nextroute
./gradlew test --tests "*.UserDomainServiceTest" 2>&1 | tail -20
```
Expected: `UserDomainService` 클래스 없어서 컴파일 에러

- [ ] **Step 5: UserDomainService 구현**

```java
// src/main/java/watoo/grd/nextroute/domain/user/service/UserDomainService.java
package watoo.grd.nextroute.domain.user.service;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import watoo.grd.nextroute.domain.user.entity.User;
import watoo.grd.nextroute.domain.user.repository.UserRepository;

@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)
public class UserDomainService {

    private final UserRepository userRepository;

    @Transactional
    public User findOrCreate(String deviceId) {
        return userRepository.findByDeviceId(deviceId)
                .orElseGet(() -> userRepository.save(
                        User.builder().deviceId(deviceId).build()
                ));
    }
}
```

- [ ] **Step 6: 테스트 실행 — PASS 확인**

```bash
./gradlew test --tests "*.UserDomainServiceTest"
```
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 7: 커밋**

```bash
git add src/main/java/watoo/grd/nextroute/domain/user/ \
        src/test/java/watoo/grd/nextroute/domain/user/
git commit -m "feat: add User deviceId + UserRepository + UserDomainService"
```

---

## Task 2: 버스 도착정보 API

**Files:**
- Modify: `src/main/java/watoo/grd/nextroute/domain/bus/repository/BusArrivalRawRepository.java`
- Modify: `src/main/java/watoo/grd/nextroute/domain/bus/service/BusDataService.java`
- Create: `src/main/java/watoo/grd/nextroute/application/arrival/dto/BusArrivalResponse.java`
- Create: `src/main/java/watoo/grd/nextroute/application/arrival/port/in/GetBusArrivalUseCase.java`
- Create: `src/main/java/watoo/grd/nextroute/application/arrival/service/BusArrivalQueryService.java`
- Create: `src/main/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/BusArrivalController.java`
- Test: `src/test/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/BusArrivalControllerTest.java`

- [ ] **Step 1: BusArrivalRawRepository에 native query 추가**

```java
// src/main/java/watoo/grd/nextroute/domain/bus/repository/BusArrivalRawRepository.java
package watoo.grd.nextroute.domain.bus.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import watoo.grd.nextroute.domain.bus.entity.BusArrivalRaw;

import java.time.LocalDateTime;
import java.util.List;

public interface BusArrivalRawRepository extends JpaRepository<BusArrivalRaw, Long> {

    List<BusArrivalRaw> findByRouteIdAndCollectedAtBetween(
            String routeId, LocalDateTime from, LocalDateTime to);

    // 정류소별 노선 최신 1건씩 (DISTINCT ON은 PostgreSQL 전용)
    @Query(value = """
            SELECT DISTINCT ON (route_id) *
            FROM bus_arrival_raw
            WHERE stop_id = :stopId
              AND collected_at >= :from
            ORDER BY route_id, collected_at DESC
            """, nativeQuery = true)
    List<BusArrivalRaw> findLatestByStopId(
            @Param("stopId") String stopId,
            @Param("from") LocalDateTime from);
}
```

- [ ] **Step 2: BusDataService에 findLatestArrivalsByStopId 추가**

기존 파일 끝에 메서드 추가:

```java
// BusDataService.java — 기존 코드 유지, 아래 메서드만 추가
public List<BusArrivalRaw> findLatestArrivalsByStopId(String stopId, LocalDateTime from) {
    return busArrivalRawRepository.findLatestByStopId(stopId, from);
}
```

- [ ] **Step 3: BusArrivalResponse DTO 생성**

```java
// src/main/java/watoo/grd/nextroute/application/arrival/dto/BusArrivalResponse.java
package watoo.grd.nextroute.application.arrival.dto;

import lombok.Builder;
import lombok.Getter;

@Getter
@Builder
public class BusArrivalResponse {
    private String routeId;
    private String arrivalMsg1;
    private Integer predictTime1;
    private Integer congestionNum1;
    private String arrivalMsg2;
    private Integer predictTime2;
    private Integer congestionNum2;
}
```

- [ ] **Step 4: GetBusArrivalUseCase Port In 생성**

```java
// src/main/java/watoo/grd/nextroute/application/arrival/port/in/GetBusArrivalUseCase.java
package watoo.grd.nextroute.application.arrival.port.in;

import watoo.grd.nextroute.application.arrival.dto.BusArrivalResponse;

import java.util.List;

public interface GetBusArrivalUseCase {
    List<BusArrivalResponse> getArrivals(String stopId);
}
```

- [ ] **Step 5: BusArrivalQueryService 테스트 작성 (실패 확인)**

```java
// src/test/java/watoo/grd/nextroute/application/arrival/service/BusArrivalQueryServiceTest.java
package watoo.grd.nextroute.application.arrival.service;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import watoo.grd.nextroute.application.arrival.dto.BusArrivalResponse;
import watoo.grd.nextroute.domain.bus.entity.BusArrivalRaw;
import watoo.grd.nextroute.domain.bus.service.BusDataService;

import java.time.LocalDateTime;
import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.eq;
import static org.mockito.BDDMockito.given;

@ExtendWith(MockitoExtension.class)
class BusArrivalQueryServiceTest {

    @Mock BusDataService busDataService;
    @InjectMocks BusArrivalQueryService service;

    @Test
    void getArrivals_returnsResponses() {
        BusArrivalRaw raw = BusArrivalRaw.builder()
                .routeId("100100001")
                .arrivalMsg1("3분 후")
                .predictTime1(180)
                .congestionNum1(3)
                .arrivalMsg2("8분 후")
                .predictTime2(480)
                .congestionNum2(2)
                .collectedAt(LocalDateTime.now())
                .stopId("22000")
                .build();

        given(busDataService.findLatestArrivalsByStopId(eq("22000"), any())).willReturn(List.of(raw));

        List<BusArrivalResponse> result = service.getArrivals("22000");

        assertThat(result).hasSize(1);
        assertThat(result.get(0).getRouteId()).isEqualTo("100100001");
        assertThat(result.get(0).getPredictTime1()).isEqualTo(180);
    }

    @Test
    void getArrivals_emptyWhenNoData() {
        given(busDataService.findLatestArrivalsByStopId(any(), any())).willReturn(List.of());

        List<BusArrivalResponse> result = service.getArrivals("00000");

        assertThat(result).isEmpty();
    }
}
```

- [ ] **Step 6: 테스트 실행 — FAIL 확인**

```bash
./gradlew test --tests "*.BusArrivalQueryServiceTest" 2>&1 | tail -10
```

- [ ] **Step 7: BusArrivalQueryService 구현**

```java
// src/main/java/watoo/grd/nextroute/application/arrival/service/BusArrivalQueryService.java
package watoo.grd.nextroute.application.arrival.service;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import watoo.grd.nextroute.application.arrival.dto.BusArrivalResponse;
import watoo.grd.nextroute.application.arrival.port.in.GetBusArrivalUseCase;
import watoo.grd.nextroute.domain.bus.service.BusDataService;

import java.time.LocalDateTime;
import java.util.List;

@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)
public class BusArrivalQueryService implements GetBusArrivalUseCase {

    private final BusDataService busDataService;
    private static final int WINDOW_MINUTES = 15;

    @Override
    public List<BusArrivalResponse> getArrivals(String stopId) {
        LocalDateTime from = LocalDateTime.now().minusMinutes(WINDOW_MINUTES);
        return busDataService.findLatestArrivalsByStopId(stopId, from).stream()
                .map(raw -> BusArrivalResponse.builder()
                        .routeId(raw.getRouteId())
                        .arrivalMsg1(raw.getArrivalMsg1())
                        .predictTime1(raw.getPredictTime1())
                        .congestionNum1(raw.getCongestionNum1())
                        .arrivalMsg2(raw.getArrivalMsg2())
                        .predictTime2(raw.getPredictTime2())
                        .congestionNum2(raw.getCongestionNum2())
                        .build())
                .toList();
    }
}
```

- [ ] **Step 8: BusArrivalController 생성**

```java
// src/main/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/BusArrivalController.java
package watoo.grd.nextroute.infrastructure.adapter.in.api.arrival;

import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import watoo.grd.nextroute.application.arrival.dto.BusArrivalResponse;
import watoo.grd.nextroute.application.arrival.port.in.GetBusArrivalUseCase;

import java.util.List;

@RestController
@RequestMapping("/api/arrivals/bus")
@RequiredArgsConstructor
@Tag(name = "[공용] 버스 도착정보")
public class BusArrivalController {

    private final GetBusArrivalUseCase getBusArrivalUseCase;

    @GetMapping("/{stopId}")
    public ResponseEntity<List<BusArrivalResponse>> getArrivals(@PathVariable String stopId) {
        return ResponseEntity.ok(getBusArrivalUseCase.getArrivals(stopId));
    }
}
```

- [ ] **Step 9: Controller 슬라이스 테스트**

```java
// src/test/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/BusArrivalControllerTest.java
package watoo.grd.nextroute.infrastructure.adapter.in.api.arrival;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.web.servlet.MockMvc;
import watoo.grd.nextroute.application.arrival.dto.BusArrivalResponse;
import watoo.grd.nextroute.application.arrival.port.in.GetBusArrivalUseCase;

import java.util.List;

import static org.mockito.BDDMockito.given;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

@WebMvcTest(BusArrivalController.class)
class BusArrivalControllerTest {

    @Autowired MockMvc mockMvc;
    @MockBean GetBusArrivalUseCase getBusArrivalUseCase;

    @Test
    void getArrivals_returns200WithList() throws Exception {
        given(getBusArrivalUseCase.getArrivals("22000")).willReturn(List.of(
                BusArrivalResponse.builder()
                        .routeId("100100001")
                        .arrivalMsg1("3분 후")
                        .predictTime1(180)
                        .build()
        ));

        mockMvc.perform(get("/api/arrivals/bus/22000"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$[0].routeId").value("100100001"))
                .andExpect(jsonPath("$[0].predictTime1").value(180));
    }

    @Test
    void getArrivals_emptyList_returns200() throws Exception {
        given(getBusArrivalUseCase.getArrivals("99999")).willReturn(List.of());

        mockMvc.perform(get("/api/arrivals/bus/99999"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$").isArray())
                .andExpect(jsonPath("$").isEmpty());
    }
}
```

- [ ] **Step 10: 전체 테스트 실행 — PASS 확인**

```bash
./gradlew test --tests "*.BusArrivalQueryServiceTest" --tests "*.BusArrivalControllerTest"
```
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 11: 커밋**

```bash
git add src/main/java/watoo/grd/nextroute/application/arrival/ \
        src/main/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/BusArrivalController.java \
        src/main/java/watoo/grd/nextroute/domain/bus/repository/BusArrivalRawRepository.java \
        src/main/java/watoo/grd/nextroute/domain/bus/service/BusDataService.java \
        src/test/java/watoo/grd/nextroute/application/arrival/ \
        src/test/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/
git commit -m "feat: add bus arrival API GET /api/arrivals/bus/{stopId}"
```

---

## Task 3: 지하철 도착정보 API

**Files:**
- Modify: `src/main/java/watoo/grd/nextroute/domain/subway/repository/SubwayArrivalRawRepository.java`
- Modify: `src/main/java/watoo/grd/nextroute/domain/subway/service/SubwayDataService.java`
- Create: `src/main/java/watoo/grd/nextroute/application/arrival/dto/SubwayArrivalResponse.java`
- Create: `src/main/java/watoo/grd/nextroute/application/arrival/port/in/GetSubwayArrivalUseCase.java`
- Create: `src/main/java/watoo/grd/nextroute/application/arrival/service/SubwayArrivalQueryService.java`
- Create: `src/main/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/SubwayArrivalController.java`
- Test: `src/test/java/watoo/grd/nextroute/application/arrival/service/SubwayArrivalQueryServiceTest.java`
- Test: `src/test/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/SubwayArrivalControllerTest.java`

- [ ] **Step 1: SubwayArrivalRawRepository에 메서드 추가**

```java
// src/main/java/watoo/grd/nextroute/domain/subway/repository/SubwayArrivalRawRepository.java
package watoo.grd.nextroute.domain.subway.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import watoo.grd.nextroute.domain.subway.entity.SubwayArrivalRaw;

import java.time.LocalDateTime;
import java.util.List;

public interface SubwayArrivalRawRepository extends JpaRepository<SubwayArrivalRaw, Long> {

    List<SubwayArrivalRaw> findByStationIdAndCollectedAtBetween(
            String stationId, LocalDateTime from, LocalDateTime to);

    List<SubwayArrivalRaw> findByStationIdAndCollectedAtAfter(
            String stationId, LocalDateTime from);
}
```

- [ ] **Step 2: SubwayDataService에 findLatestArrivalsByStationId 추가**

기존 파일 끝에 메서드 추가:

```java
// SubwayDataService.java — 기존 코드 유지, 아래 메서드만 추가
public List<SubwayArrivalRaw> findLatestArrivalsByStationId(String stationId, LocalDateTime from) {
    return subwayArrivalRawRepository.findByStationIdAndCollectedAtAfter(stationId, from);
}
```

- [ ] **Step 3: SubwayArrivalResponse DTO 생성**

```java
// src/main/java/watoo/grd/nextroute/application/arrival/dto/SubwayArrivalResponse.java
package watoo.grd.nextroute.application.arrival.dto;

import lombok.Builder;
import lombok.Getter;

@Getter
@Builder
public class SubwayArrivalResponse {
    private String lineId;
    private String direction;
    private Integer arrivalSeconds;
    private String currentMessage;
    private String destinationName;
    private String trainType;
    private String arrivalCode;
}
```

- [ ] **Step 4: GetSubwayArrivalUseCase Port In 생성**

```java
// src/main/java/watoo/grd/nextroute/application/arrival/port/in/GetSubwayArrivalUseCase.java
package watoo.grd.nextroute.application.arrival.port.in;

import watoo.grd.nextroute.application.arrival.dto.SubwayArrivalResponse;

import java.util.List;

public interface GetSubwayArrivalUseCase {
    List<SubwayArrivalResponse> getArrivals(String stationId);
}
```

- [ ] **Step 5: SubwayArrivalQueryService 테스트 작성 (실패 확인)**

```java
// src/test/java/watoo/grd/nextroute/application/arrival/service/SubwayArrivalQueryServiceTest.java
package watoo.grd.nextroute.application.arrival.service;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import watoo.grd.nextroute.application.arrival.dto.SubwayArrivalResponse;
import watoo.grd.nextroute.domain.subway.entity.SubwayArrivalRaw;
import watoo.grd.nextroute.domain.subway.service.SubwayDataService;

import java.time.LocalDateTime;
import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.eq;
import static org.mockito.BDDMockito.given;

@ExtendWith(MockitoExtension.class)
class SubwayArrivalQueryServiceTest {

    @Mock SubwayDataService subwayDataService;
    @InjectMocks SubwayArrivalQueryService service;

    @Test
    void getArrivals_deduplicatesByDirectionAndTrainNo() {
        LocalDateTime now = LocalDateTime.now();
        // 같은 열차 2번 수집 → 최신 1건만
        SubwayArrivalRaw old = SubwayArrivalRaw.builder()
                .stationId("1002000233").lineId("1002").direction("상행")
                .trainNo("2001").arrivalSeconds(120).currentMessage("2정거장 전")
                .collectedAt(now.minusMinutes(3)).build();
        SubwayArrivalRaw recent = SubwayArrivalRaw.builder()
                .stationId("1002000233").lineId("1002").direction("상행")
                .trainNo("2001").arrivalSeconds(60).currentMessage("1정거장 전")
                .collectedAt(now.minusMinutes(1)).build();

        given(subwayDataService.findLatestArrivalsByStationId(eq("1002000233"), any()))
                .willReturn(List.of(old, recent));

        List<SubwayArrivalResponse> result = service.getArrivals("1002000233");

        assertThat(result).hasSize(1);
        assertThat(result.get(0).getArrivalSeconds()).isEqualTo(60);
    }

    @Test
    void getArrivals_sortsByArrivalSeconds() {
        LocalDateTime now = LocalDateTime.now();
        SubwayArrivalRaw far = SubwayArrivalRaw.builder()
                .stationId("1002000233").lineId("1002").direction("상행")
                .trainNo("2002").arrivalSeconds(300).collectedAt(now).build();
        SubwayArrivalRaw near = SubwayArrivalRaw.builder()
                .stationId("1002000233").lineId("1002").direction("상행")
                .trainNo("2001").arrivalSeconds(60).collectedAt(now).build();

        given(subwayDataService.findLatestArrivalsByStationId(eq("1002000233"), any()))
                .willReturn(List.of(far, near));

        List<SubwayArrivalResponse> result = service.getArrivals("1002000233");

        assertThat(result.get(0).getArrivalSeconds()).isEqualTo(60);
        assertThat(result.get(1).getArrivalSeconds()).isEqualTo(300);
    }
}
```

- [ ] **Step 6: 테스트 실행 — FAIL 확인**

```bash
./gradlew test --tests "*.SubwayArrivalQueryServiceTest" 2>&1 | tail -10
```

- [ ] **Step 7: SubwayArrivalQueryService 구현**

```java
// src/main/java/watoo/grd/nextroute/application/arrival/service/SubwayArrivalQueryService.java
package watoo.grd.nextroute.application.arrival.service;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import watoo.grd.nextroute.application.arrival.dto.SubwayArrivalResponse;
import watoo.grd.nextroute.application.arrival.port.in.GetSubwayArrivalUseCase;
import watoo.grd.nextroute.domain.subway.entity.SubwayArrivalRaw;
import watoo.grd.nextroute.domain.subway.service.SubwayDataService;

import java.time.LocalDateTime;
import java.util.*;

@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)
public class SubwayArrivalQueryService implements GetSubwayArrivalUseCase {

    private final SubwayDataService subwayDataService;
    private static final int WINDOW_MINUTES = 5;

    @Override
    public List<SubwayArrivalResponse> getArrivals(String stationId) {
        LocalDateTime from = LocalDateTime.now().minusMinutes(WINDOW_MINUTES);
        List<SubwayArrivalRaw> raw = subwayDataService.findLatestArrivalsByStationId(stationId, from);

        // (direction, trainNo) 기준 최신 수집 1건만
        Map<String, SubwayArrivalRaw> deduped = new LinkedHashMap<>();
        raw.stream()
                .sorted(Comparator.comparing(SubwayArrivalRaw::getCollectedAt).reversed())
                .forEach(r -> {
                    String key = r.getDirection() + ":" + r.getTrainNo();
                    deduped.putIfAbsent(key, r);
                });

        return deduped.values().stream()
                .sorted(Comparator.comparingInt(r ->
                        Optional.ofNullable(r.getArrivalSeconds()).orElse(Integer.MAX_VALUE)))
                .map(r -> SubwayArrivalResponse.builder()
                        .lineId(r.getLineId())
                        .direction(r.getDirection())
                        .arrivalSeconds(r.getArrivalSeconds())
                        .currentMessage(r.getCurrentMessage())
                        .destinationName(r.getDestinationName())
                        .trainType(r.getTrainType())
                        .arrivalCode(r.getArrivalCode())
                        .build())
                .toList();
    }
}
```

- [ ] **Step 8: SubwayArrivalController 생성**

```java
// src/main/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/SubwayArrivalController.java
package watoo.grd.nextroute.infrastructure.adapter.in.api.arrival;

import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import watoo.grd.nextroute.application.arrival.dto.SubwayArrivalResponse;
import watoo.grd.nextroute.application.arrival.port.in.GetSubwayArrivalUseCase;

import java.util.List;

@RestController
@RequestMapping("/api/arrivals/subway")
@RequiredArgsConstructor
@Tag(name = "[공용] 지하철 도착정보")
public class SubwayArrivalController {

    private final GetSubwayArrivalUseCase getSubwayArrivalUseCase;

    @GetMapping("/{stationId}")
    public ResponseEntity<List<SubwayArrivalResponse>> getArrivals(@PathVariable String stationId) {
        return ResponseEntity.ok(getSubwayArrivalUseCase.getArrivals(stationId));
    }
}
```

- [ ] **Step 9: Controller 슬라이스 테스트**

```java
// src/test/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/SubwayArrivalControllerTest.java
package watoo.grd.nextroute.infrastructure.adapter.in.api.arrival;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.web.servlet.MockMvc;
import watoo.grd.nextroute.application.arrival.dto.SubwayArrivalResponse;
import watoo.grd.nextroute.application.arrival.port.in.GetSubwayArrivalUseCase;

import java.util.List;

import static org.mockito.BDDMockito.given;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

@WebMvcTest(SubwayArrivalController.class)
class SubwayArrivalControllerTest {

    @Autowired MockMvc mockMvc;
    @MockBean GetSubwayArrivalUseCase getSubwayArrivalUseCase;

    @Test
    void getArrivals_returns200WithList() throws Exception {
        given(getSubwayArrivalUseCase.getArrivals("1002000233")).willReturn(List.of(
                SubwayArrivalResponse.builder()
                        .lineId("1002").direction("상행")
                        .arrivalSeconds(60).currentMessage("1정거장 전")
                        .destinationName("성수행").build()
        ));

        mockMvc.perform(get("/api/arrivals/subway/1002000233"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$[0].lineId").value("1002"))
                .andExpect(jsonPath("$[0].arrivalSeconds").value(60));
    }
}
```

- [ ] **Step 10: 전체 테스트 실행 — PASS 확인**

```bash
./gradlew test --tests "*.SubwayArrivalQueryServiceTest" --tests "*.SubwayArrivalControllerTest"
```

- [ ] **Step 11: 커밋**

```bash
git add src/main/java/watoo/grd/nextroute/application/arrival/ \
        src/main/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/ \
        src/main/java/watoo/grd/nextroute/domain/subway/ \
        src/test/java/watoo/grd/nextroute/application/arrival/ \
        src/test/java/watoo/grd/nextroute/infrastructure/adapter/in/api/arrival/
git commit -m "feat: add subway arrival API GET /api/arrivals/subway/{stationId}"
```

---

## Task 4: 즐겨찾기 도메인 정리

**Files:**
- Modify: `src/main/java/watoo/grd/nextroute/domain/route/favorite/entity/FavoriteRoute.java`
- Modify: `src/main/java/watoo/grd/nextroute/domain/route/favorite/repository/FavoriteRouteRepository.java`
- Modify: `src/main/java/watoo/grd/nextroute/domain/route/favorite/service/FavoriteRouteService.java`

- [ ] **Step 1: FavoriteRoute 엔티티 정리**

기존 엔티티의 문제: `User UserId` (필드명이 타입명 혼재), `@Table` 이름 없음, `@Builder` 없음

```java
// src/main/java/watoo/grd/nextroute/domain/route/favorite/entity/FavoriteRoute.java
package watoo.grd.nextroute.domain.route.favorite.entity;

import jakarta.persistence.*;
import lombok.AccessLevel;
import lombok.Builder;
import lombok.Getter;
import lombok.NoArgsConstructor;
import watoo.grd.nextroute.common.entity.BaseEntity;
import watoo.grd.nextroute.domain.user.entity.User;

import java.time.LocalDateTime;

@Entity
@Table(name = "favorite_route")
@Getter
@NoArgsConstructor(access = AccessLevel.PROTECTED)
public class FavoriteRoute extends BaseEntity {

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "user_id", nullable = false)
    private User user;

    @Column(nullable = false)
    @Enumerated(EnumType.STRING)
    private FavoriteType type;

    @Column(nullable = false)
    private String endPlace;

    @Column(nullable = false)
    private Double ex;

    @Column(nullable = false)
    private Double ey;

    private LocalDateTime endDate;

    @Builder
    public FavoriteRoute(User user, FavoriteType type, String endPlace,
                         Double ex, Double ey, LocalDateTime endDate) {
        this.user = user;
        this.type = type;
        this.endPlace = endPlace;
        this.ex = ex;
        this.ey = ey;
        this.endDate = endDate;
    }
}
```

- [ ] **Step 2: FavoriteRouteRepository 수정**

```java
// src/main/java/watoo/grd/nextroute/domain/route/favorite/repository/FavoriteRouteRepository.java
package watoo.grd.nextroute.domain.route.favorite.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import watoo.grd.nextroute.domain.route.favorite.entity.FavoriteRoute;
import watoo.grd.nextroute.domain.user.entity.User;

import java.util.List;
import java.util.Optional;

public interface FavoriteRouteRepository extends JpaRepository<FavoriteRoute, Long> {

    List<FavoriteRoute> findByUserAndDeletedAtIsNull(User user);

    Optional<FavoriteRoute> findByIdAndUserAndDeletedAtIsNull(Long id, User user);
}
```

- [ ] **Step 3: FavoriteRoute 도메인 서비스 완성**

```java
// src/main/java/watoo/grd/nextroute/domain/route/favorite/service/FavoriteRouteService.java
package watoo.grd.nextroute.domain.route.favorite.service;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import watoo.grd.nextroute.domain.route.favorite.entity.FavoriteRoute;
import watoo.grd.nextroute.domain.route.favorite.repository.FavoriteRouteRepository;
import watoo.grd.nextroute.domain.user.entity.User;

import java.util.List;
import java.util.Optional;

@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)
public class FavoriteRouteService {

    private final FavoriteRouteRepository favoriteRouteRepository;

    @Transactional
    public FavoriteRoute save(FavoriteRoute favoriteRoute) {
        return favoriteRouteRepository.save(favoriteRoute);
    }

    public List<FavoriteRoute> findByUser(User user) {
        return favoriteRouteRepository.findByUserAndDeletedAtIsNull(user);
    }

    public Optional<FavoriteRoute> findByIdAndUser(Long id, User user) {
        return favoriteRouteRepository.findByIdAndUserAndDeletedAtIsNull(id, user);
    }
}
```

> 주의: `BaseEntity.deletedAt`은 논리삭제용. 삭제는 Task 5에서 `deletedAt`을 현재 시각으로 set. 현재 BaseEntity에 setter가 없으므로 Task 5에서 `@Setter` 없이 처리하는 방법을 사용한다 (아래 참고).

- [ ] **Step 4: 빌드 확인**

```bash
./gradlew compileJava 2>&1 | tail -20
```
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 5: 커밋**

```bash
git add src/main/java/watoo/grd/nextroute/domain/route/favorite/
git commit -m "refactor: fix FavoriteRoute entity + add FavoriteRoute domain service CRUD"
```

---

## Task 5: 즐겨찾기 API

**Files:**
- Modify: `src/main/java/watoo/grd/nextroute/application/route/port/in/AddFavoriteRouteUseCase.java`
- Create: `src/main/java/watoo/grd/nextroute/application/route/port/in/GetFavoriteRoutesUseCase.java`
- Create: `src/main/java/watoo/grd/nextroute/application/route/port/in/DeleteFavoriteRouteUseCase.java`
- Create: `src/main/java/watoo/grd/nextroute/application/route/dto/FavoriteRequest.java`
- Create: `src/main/java/watoo/grd/nextroute/application/route/dto/FavoriteResponse.java`
- Modify: `src/main/java/watoo/grd/nextroute/application/route/service/FavoriteRouteService.java`
- Modify: `src/main/java/watoo/grd/nextroute/infrastructure/adapter/in/api/route/favorite/FavoriteController.java`
- Test: `src/test/java/watoo/grd/nextroute/infrastructure/adapter/in/api/route/favorite/FavoriteControllerTest.java`

- [ ] **Step 1: UseCase 인터페이스 정의**

```java
// AddFavoriteRouteUseCase.java — 기존 파일 전체 교체
package watoo.grd.nextroute.application.route.port.in;

import watoo.grd.nextroute.application.route.dto.FavoriteRequest;
import watoo.grd.nextroute.application.route.dto.FavoriteResponse;

public interface AddFavoriteRouteUseCase {
    FavoriteResponse add(String deviceId, FavoriteRequest request);
}
```

```java
// GetFavoriteRoutesUseCase.java
package watoo.grd.nextroute.application.route.port.in;

import watoo.grd.nextroute.application.route.dto.FavoriteResponse;

import java.util.List;

public interface GetFavoriteRoutesUseCase {
    List<FavoriteResponse> getAll(String deviceId);
}
```

```java
// DeleteFavoriteRouteUseCase.java
package watoo.grd.nextroute.application.route.port.in;

public interface DeleteFavoriteRouteUseCase {
    void delete(String deviceId, Long favoriteId);
}
```

- [ ] **Step 2: DTO 생성**

```java
// src/main/java/watoo/grd/nextroute/application/route/dto/FavoriteRequest.java
package watoo.grd.nextroute.application.route.dto;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;
import lombok.Getter;
import watoo.grd.nextroute.domain.route.favorite.entity.FavoriteType;

import java.time.LocalDateTime;

@Getter
public class FavoriteRequest {
    @NotNull
    private FavoriteType type;
    @NotBlank
    private String endPlace;
    @NotNull
    private Double ex;
    @NotNull
    private Double ey;
    private LocalDateTime endDate;
}
```

```java
// src/main/java/watoo/grd/nextroute/application/route/dto/FavoriteResponse.java
package watoo.grd.nextroute.application.route.dto;

import lombok.Builder;
import lombok.Getter;
import watoo.grd.nextroute.domain.route.favorite.entity.FavoriteRoute;
import watoo.grd.nextroute.domain.route.favorite.entity.FavoriteType;

import java.time.LocalDateTime;

@Getter
@Builder
public class FavoriteResponse {
    private Long id;
    private FavoriteType type;
    private String endPlace;
    private Double ex;
    private Double ey;
    private LocalDateTime endDate;

    public static FavoriteResponse from(FavoriteRoute entity) {
        return FavoriteResponse.builder()
                .id(entity.getId())
                .type(entity.getType())
                .endPlace(entity.getEndPlace())
                .ex(entity.getEx())
                .ey(entity.getEy())
                .endDate(entity.getEndDate())
                .build();
    }
}
```

- [ ] **Step 3: BaseEntity에 논리삭제 메서드 추가**

삭제 시 `deletedAt`을 set해야 한다. BaseEntity에 `markDeleted()` 추가:

```java
// BaseEntity.java — 기존 코드 유지, 아래 메서드만 추가
public void markDeleted() {
    this.deletedAt = LocalDateTime.now();
}
```

- [ ] **Step 4: Application FavoriteRouteService 완성**

```java
// src/main/java/watoo/grd/nextroute/application/route/service/FavoriteRouteService.java
package watoo.grd.nextroute.application.route.service;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import watoo.grd.nextroute.application.route.dto.FavoriteRequest;
import watoo.grd.nextroute.application.route.dto.FavoriteResponse;
import watoo.grd.nextroute.application.route.port.in.AddFavoriteRouteUseCase;
import watoo.grd.nextroute.application.route.port.in.DeleteFavoriteRouteUseCase;
import watoo.grd.nextroute.application.route.port.in.GetFavoriteRoutesUseCase;
import watoo.grd.nextroute.domain.route.favorite.entity.FavoriteRoute;
import watoo.grd.nextroute.domain.route.favorite.service.FavoriteRouteService;
import watoo.grd.nextroute.domain.user.entity.User;
import watoo.grd.nextroute.domain.user.service.UserDomainService;

import java.util.List;
import java.util.NoSuchElementException;

@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)
public class FavoriteRouteService
        implements AddFavoriteRouteUseCase, GetFavoriteRoutesUseCase, DeleteFavoriteRouteUseCase {

    private final watoo.grd.nextroute.domain.route.favorite.service.FavoriteRouteService favoriteDomainService;
    private final UserDomainService userDomainService;

    @Override
    @Transactional
    public FavoriteResponse add(String deviceId, FavoriteRequest request) {
        User user = userDomainService.findOrCreate(deviceId);
        FavoriteRoute saved = favoriteDomainService.save(
                FavoriteRoute.builder()
                        .user(user)
                        .type(request.getType())
                        .endPlace(request.getEndPlace())
                        .ex(request.getEx())
                        .ey(request.getEy())
                        .endDate(request.getEndDate())
                        .build()
        );
        return FavoriteResponse.from(saved);
    }

    @Override
    public List<FavoriteResponse> getAll(String deviceId) {
        User user = userDomainService.findOrCreate(deviceId);
        return favoriteDomainService.findByUser(user).stream()
                .map(FavoriteResponse::from)
                .toList();
    }

    @Override
    @Transactional
    public void delete(String deviceId, Long favoriteId) {
        User user = userDomainService.findOrCreate(deviceId);
        FavoriteRoute route = favoriteDomainService.findByIdAndUser(favoriteId, user)
                .orElseThrow(() -> new NoSuchElementException("즐겨찾기를 찾을 수 없습니다."));
        route.markDeleted();
    }
}
```

- [ ] **Step 5: FavoriteController 완성**

```java
// src/main/java/watoo/grd/nextroute/infrastructure/adapter/in/api/route/favorite/FavoriteController.java
package watoo.grd.nextroute.infrastructure.adapter.in.api.route.favorite;

import io.swagger.v3.oas.annotations.tags.Tag;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import watoo.grd.nextroute.application.route.dto.FavoriteRequest;
import watoo.grd.nextroute.application.route.dto.FavoriteResponse;
import watoo.grd.nextroute.application.route.port.in.AddFavoriteRouteUseCase;
import watoo.grd.nextroute.application.route.port.in.DeleteFavoriteRouteUseCase;
import watoo.grd.nextroute.application.route.port.in.GetFavoriteRoutesUseCase;

import java.util.List;
import java.util.NoSuchElementException;

@RestController
@RequestMapping("/api/route/fav")
@RequiredArgsConstructor
@Tag(name = "[회원] 경로 즐겨찾기(집, 회사)")
public class FavoriteController {

    private final AddFavoriteRouteUseCase addFavoriteRouteUseCase;
    private final GetFavoriteRoutesUseCase getFavoriteRoutesUseCase;
    private final DeleteFavoriteRouteUseCase deleteFavoriteRouteUseCase;

    @PostMapping
    public ResponseEntity<FavoriteResponse> add(
            @RequestHeader("X-Device-Id") String deviceId,
            @Valid @RequestBody FavoriteRequest request) {
        return ResponseEntity.ok(addFavoriteRouteUseCase.add(deviceId, request));
    }

    @GetMapping
    public ResponseEntity<List<FavoriteResponse>> getAll(
            @RequestHeader("X-Device-Id") String deviceId) {
        return ResponseEntity.ok(getFavoriteRoutesUseCase.getAll(deviceId));
    }

    @DeleteMapping("/{id}")
    public ResponseEntity<Void> delete(
            @RequestHeader("X-Device-Id") String deviceId,
            @PathVariable Long id) {
        deleteFavoriteRouteUseCase.delete(deviceId, id);
        return ResponseEntity.noContent().build();
    }

    @ExceptionHandler(NoSuchElementException.class)
    public ResponseEntity<Void> handleNotFound() {
        return ResponseEntity.notFound().build();
    }
}
```

- [ ] **Step 6: FavoriteController 테스트**

```java
// src/test/java/watoo/grd/nextroute/infrastructure/adapter/in/api/route/favorite/FavoriteControllerTest.java
package watoo.grd.nextroute.infrastructure.adapter.in.api.route.favorite;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;
import watoo.grd.nextroute.application.route.dto.FavoriteRequest;
import watoo.grd.nextroute.application.route.dto.FavoriteResponse;
import watoo.grd.nextroute.application.route.port.in.AddFavoriteRouteUseCase;
import watoo.grd.nextroute.application.route.port.in.DeleteFavoriteRouteUseCase;
import watoo.grd.nextroute.application.route.port.in.GetFavoriteRoutesUseCase;
import watoo.grd.nextroute.domain.route.favorite.entity.FavoriteType;

import java.util.List;
import java.util.NoSuchElementException;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.eq;
import static org.mockito.BDDMockito.given;
import static org.mockito.BDDMockito.willThrow;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

@WebMvcTest(FavoriteController.class)
class FavoriteControllerTest {

    @Autowired MockMvc mockMvc;
    @Autowired ObjectMapper objectMapper;
    @MockBean AddFavoriteRouteUseCase addFavoriteRouteUseCase;
    @MockBean GetFavoriteRoutesUseCase getFavoriteRoutesUseCase;
    @MockBean DeleteFavoriteRouteUseCase deleteFavoriteRouteUseCase;

    @Test
    void add_returns200WithResponse() throws Exception {
        FavoriteResponse response = FavoriteResponse.builder()
                .id(1L).type(FavoriteType.HOME).endPlace("집")
                .ex(127.1).ey(37.5).build();
        given(addFavoriteRouteUseCase.add(eq("device-1"), any())).willReturn(response);

        String body = """
                {"type":"HOME","endPlace":"집","ex":127.1,"ey":37.5}
                """;

        mockMvc.perform(post("/api/route/fav")
                        .header("X-Device-Id", "device-1")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content(body))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.id").value(1))
                .andExpect(jsonPath("$.endPlace").value("집"));
    }

    @Test
    void getAll_returns200WithList() throws Exception {
        given(getFavoriteRoutesUseCase.getAll("device-1")).willReturn(List.of(
                FavoriteResponse.builder().id(1L).type(FavoriteType.HOME).endPlace("집")
                        .ex(127.1).ey(37.5).build()
        ));

        mockMvc.perform(get("/api/route/fav").header("X-Device-Id", "device-1"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$[0].type").value("HOME"));
    }

    @Test
    void delete_returns204() throws Exception {
        mockMvc.perform(delete("/api/route/fav/1").header("X-Device-Id", "device-1"))
                .andExpect(status().isNoContent());
    }

    @Test
    void delete_notFound_returns404() throws Exception {
        willThrow(new NoSuchElementException()).given(deleteFavoriteRouteUseCase).delete(any(), eq(99L));

        mockMvc.perform(delete("/api/route/fav/99").header("X-Device-Id", "device-1"))
                .andExpect(status().isNotFound());
    }
}
```

- [ ] **Step 7: 전체 테스트 실행 — PASS 확인**

```bash
./gradlew test --tests "*.FavoriteControllerTest"
```

- [ ] **Step 8: 커밋**

```bash
git add src/main/java/watoo/grd/nextroute/application/route/ \
        src/main/java/watoo/grd/nextroute/infrastructure/adapter/in/api/route/favorite/ \
        src/main/java/watoo/grd/nextroute/common/entity/BaseEntity.java \
        src/test/java/watoo/grd/nextroute/infrastructure/adapter/in/api/route/favorite/
git commit -m "feat: add favorites CRUD API POST/GET/DELETE /api/route/fav"
```

---

## Task 6: CORS 설정 + 전체 빌드 확인

**Files:**
- Create: `src/main/java/watoo/grd/nextroute/infrastructure/config/CorsConfig.java`

- [ ] **Step 1: CorsConfig 생성**

Expo 웹 export 시 Vercel 도메인에서 API를 호출하므로 CORS 필요.
`ALLOWED_ORIGINS` 환경변수로 도메인을 주입받아 유연하게 관리.

```java
// src/main/java/watoo/grd/nextroute/infrastructure/config/CorsConfig.java
package watoo.grd.nextroute.infrastructure.config;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class CorsConfig implements WebMvcConfigurer {

    @Value("${cors.allowed-origins:*}")
    private String allowedOrigins;

    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/api/**")
                .allowedOriginPatterns(allowedOrigins)
                .allowedMethods("GET", "POST", "DELETE", "OPTIONS")
                .allowedHeaders("*")
                .allowCredentials(false)
                .maxAge(3600);
    }
}
```

`env/api.env`에 추가:
```
CORS_ALLOWED_ORIGINS=https://your-app.vercel.app
```

application.properties에 추가:
```properties
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
```

- [ ] **Step 2: 전체 빌드 + 테스트 PASS 확인**

```bash
./gradlew build 2>&1 | tail -20
```
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 3: 커밋**

```bash
git add src/main/java/watoo/grd/nextroute/infrastructure/config/CorsConfig.java
git commit -m "feat: add CORS config for Expo web client"
```

---

## Task 7: Expo 프로젝트 셋업 + API 클라이언트

**위치:** `/Users/wa/golbob/개인/nextroute-app/`

- [ ] **Step 1: Expo 프로젝트 생성**

```bash
cd /Users/wa/golbob/개인
npx create-expo-app nextroute-app --template tabs
cd nextroute-app
```

- [ ] **Step 2: 의존성 설치**

```bash
npx expo install expo-location
npm install axios
npm install @react-native-async-storage/async-storage
npx expo install expo-router
```

- [ ] **Step 3: constants/Config.ts 생성**

```typescript
// constants/Config.ts
export const API_BASE_URL =
  process.env.EXPO_PUBLIC_API_BASE_URL ?? 'http://localhost:8080';

export const KAKAO_REST_API_KEY =
  process.env.EXPO_PUBLIC_KAKAO_REST_API_KEY ?? '';
```

`.env.local` 파일 (git ignore):
```
EXPO_PUBLIC_API_BASE_URL=https://your-vps-domain.com
EXPO_PUBLIC_KAKAO_REST_API_KEY=your_kakao_key
```

- [ ] **Step 4: lib/storage.ts 생성**

```typescript
// lib/storage.ts
import AsyncStorage from '@react-native-async-storage/async-storage';
import * as Crypto from 'expo-crypto';

const DEVICE_ID_KEY = 'nextroute_device_id';

export async function getDeviceId(): Promise<string> {
  let id = await AsyncStorage.getItem(DEVICE_ID_KEY);
  if (!id) {
    id = Crypto.randomUUID();
    await AsyncStorage.setItem(DEVICE_ID_KEY, id);
  }
  return id;
}
```

`expo-crypto` 설치:
```bash
npx expo install expo-crypto
```

- [ ] **Step 5: lib/api.ts 생성**

```typescript
// lib/api.ts
import axios from 'axios';
import { API_BASE_URL } from '../constants/Config';
import { getDeviceId } from './storage';

export const apiClient = axios.create({ baseURL: API_BASE_URL });

// 모든 요청에 X-Device-Id 자동 삽입
apiClient.interceptors.request.use(async (config) => {
  const deviceId = await getDeviceId();
  config.headers['X-Device-Id'] = deviceId;
  return config;
});

// 타입 정의
export interface RouteSearchParams {
  startX: number;
  startY: number;
  endX: number;
  endY: number;
  startName?: string;
  endName?: string;
}

export interface SubPathResult {
  trafficType: number; // 1=지하철, 2=버스, 3=도보
  sectionTime: number;
  distance?: number;
  lanes: { name: string; busNo?: string; subwayCode?: number }[];
  stations: { index: number; stationID: string; stationName: string; x: number; y: number }[];
  startName: string;
  endName: string;
}

export interface PathResult {
  pathType: number;
  info: { totalTime: number; payment: number; busTransitCount: number; subwayTransitCount: number };
  subPaths: SubPathResult[];
}

export interface RouteSearchResult {
  searchType: number;
  busCount: number;
  subwayCount: number;
  paths: PathResult[];
}

export interface BusArrivalItem {
  routeId: string;
  arrivalMsg1: string;
  predictTime1: number;
  congestionNum1: number;
  arrivalMsg2: string;
  predictTime2: number;
  congestionNum2: number;
}

export interface SubwayArrivalItem {
  lineId: string;
  direction: string;
  arrivalSeconds: number;
  currentMessage: string;
  destinationName: string;
  trainType: string;
  arrivalCode: string;
}

export interface FavoriteItem {
  id: number;
  type: 'HOME' | 'WORK';
  endPlace: string;
  ex: number;
  ey: number;
  endDate?: string;
}

// API 함수
export const searchRoute = (params: RouteSearchParams) =>
  apiClient.get<RouteSearchResult>('/api/route/search', { params });

export const getBusArrivals = (stopId: string) =>
  apiClient.get<BusArrivalItem[]>(`/api/arrivals/bus/${stopId}`);

export const getSubwayArrivals = (stationId: string) =>
  apiClient.get<SubwayArrivalItem[]>(`/api/arrivals/subway/${stationId}`);

export const getFavorites = () =>
  apiClient.get<FavoriteItem[]>('/api/route/fav');

export const addFavorite = (body: Omit<FavoriteItem, 'id'>) =>
  apiClient.post<FavoriteItem>('/api/route/fav', body);

export const deleteFavorite = (id: number) =>
  apiClient.delete(`/api/route/fav/${id}`);

// Kakao 주소 검색 (좌표 반환)
export const searchAddress = async (query: string) => {
  const { KAKAO_REST_API_KEY } = await import('../constants/Config');
  const res = await axios.get('https://dapi.kakao.com/v2/local/search/keyword.json', {
    params: { query },
    headers: { Authorization: `KakaoAK ${KAKAO_REST_API_KEY}` },
  });
  return res.data.documents as { place_name: string; x: string; y: string }[];
};
```

- [ ] **Step 6: 프로젝트 실행 확인**

```bash
npx expo start
```
Expected: Metro bundler 실행, QR 코드 출력

- [ ] **Step 7: 커밋**

```bash
cd /Users/wa/golbob/개인/nextroute-app
git init
git add .
git commit -m "chore: init Expo project with API client + storage"
```

---

## Task 8: 경로검색 화면

**Files:**
- Modify: `app/(tabs)/index.tsx`
- Create: `app/route-result.tsx`
- Create: `components/RouteCard.tsx`

- [ ] **Step 1: RouteCard 컴포넌트 생성**

```typescript
// components/RouteCard.tsx
import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';
import { PathResult } from '../lib/api';
import { useRouter } from 'expo-router';

interface Props {
  path: PathResult;
  index: number;
}

const trafficLabel = (type: number) =>
  type === 1 ? '🚇' : type === 2 ? '🚌' : '🚶';

export function RouteCard({ path, index }: Props) {
  const router = useRouter();
  const { totalTime, payment } = path.info;
  const segments = path.subPaths
    .filter((s) => s.trafficType !== 3)
    .map((s) => ({
      icon: trafficLabel(s.trafficType),
      name: s.lanes[0]?.name ?? s.lanes[0]?.busNo ?? '?',
      stationId: s.stations[0]?.stationID,
      trafficType: s.trafficType,
      startName: s.startName,
    }));

  return (
    <TouchableOpacity
      style={styles.card}
      onPress={() =>
        router.push({ pathname: '/route-result', params: { pathJson: JSON.stringify(path) } })
      }
    >
      <Text style={styles.time}>{totalTime}분</Text>
      <Text style={styles.payment}>{payment.toLocaleString()}원</Text>
      <View style={styles.segments}>
        {segments.map((seg, i) => (
          <Text key={i} style={styles.seg}>
            {seg.icon} {seg.name}
          </Text>
        ))}
      </View>
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  card: { backgroundColor: '#fff', borderRadius: 12, padding: 16, marginBottom: 12, elevation: 2 },
  time: { fontSize: 20, fontWeight: 'bold' },
  payment: { color: '#666', marginTop: 4 },
  segments: { flexDirection: 'row', flexWrap: 'wrap', marginTop: 8, gap: 8 },
  seg: { backgroundColor: '#f0f0f0', borderRadius: 6, paddingHorizontal: 8, paddingVertical: 4 },
});
```

- [ ] **Step 2: 경로검색 탭 (index.tsx) 작성**

```typescript
// app/(tabs)/index.tsx
import React, { useState } from 'react';
import {
  View, Text, TextInput, TouchableOpacity, FlatList,
  StyleSheet, ActivityIndicator, Alert,
} from 'react-native';
import * as Location from 'expo-location';
import { searchRoute, searchAddress, RouteSearchResult } from '../../lib/api';
import { RouteCard } from '../../components/RouteCard';

export default function SearchScreen() {
  const [destination, setDestination] = useState('');
  const [suggestions, setSuggestions] = useState<{ place_name: string; x: string; y: string }[]>([]);
  const [selectedDest, setSelectedDest] = useState<{ name: string; x: number; y: number } | null>(null);
  const [result, setResult] = useState<RouteSearchResult | null>(null);
  const [loading, setLoading] = useState(false);

  const handleAddressSearch = async () => {
    if (!destination.trim()) return;
    const docs = await searchAddress(destination);
    setSuggestions(docs.slice(0, 5));
  };

  const handleSelectSuggestion = (doc: { place_name: string; x: string; y: string }) => {
    setSelectedDest({ name: doc.place_name, x: parseFloat(doc.x), y: parseFloat(doc.y) });
    setSuggestions([]);
    setDestination(doc.place_name);
  };

  const handleSearch = async () => {
    if (!selectedDest) { Alert.alert('목적지를 선택해주세요'); return; }
    setLoading(true);
    try {
      const { status } = await Location.requestForegroundPermissionsAsync();
      if (status !== 'granted') { Alert.alert('위치 권한이 필요합니다'); return; }
      const loc = await Location.getCurrentPositionAsync({});
      const res = await searchRoute({
        startX: loc.coords.longitude,
        startY: loc.coords.latitude,
        endX: selectedDest.x,
        endY: selectedDest.y,
        endName: selectedDest.name,
      });
      setResult(res.data);
    } catch (e) {
      Alert.alert('검색 실패', '잠시 후 다시 시도해주세요');
    } finally {
      setLoading(false);
    }
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>경로 검색</Text>
      <View style={styles.inputRow}>
        <TextInput
          style={styles.input}
          placeholder="목적지 검색"
          value={destination}
          onChangeText={setDestination}
          onSubmitEditing={handleAddressSearch}
          returnKeyType="search"
        />
        <TouchableOpacity style={styles.btn} onPress={handleAddressSearch}>
          <Text style={styles.btnText}>검색</Text>
        </TouchableOpacity>
      </View>

      {suggestions.map((s, i) => (
        <TouchableOpacity key={i} style={styles.suggestion} onPress={() => handleSelectSuggestion(s)}>
          <Text>{s.place_name}</Text>
        </TouchableOpacity>
      ))}

      {selectedDest && (
        <TouchableOpacity style={styles.searchBtn} onPress={handleSearch}>
          <Text style={styles.searchBtnText}>길찾기</Text>
        </TouchableOpacity>
      )}

      {loading && <ActivityIndicator style={{ marginTop: 20 }} />}

      {result && (
        <FlatList
          data={result.paths}
          keyExtractor={(_, i) => String(i)}
          renderItem={({ item, index }) => <RouteCard path={item} index={index} />}
          style={{ marginTop: 12 }}
        />
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#f5f5f5', padding: 16, paddingTop: 60 },
  title: { fontSize: 24, fontWeight: 'bold', marginBottom: 16 },
  inputRow: { flexDirection: 'row', gap: 8, marginBottom: 8 },
  input: { flex: 1, borderWidth: 1, borderColor: '#ddd', borderRadius: 8, padding: 12, backgroundColor: '#fff' },
  btn: { backgroundColor: '#007AFF', borderRadius: 8, padding: 12, justifyContent: 'center' },
  btnText: { color: '#fff', fontWeight: 'bold' },
  suggestion: { padding: 12, backgroundColor: '#fff', borderBottomWidth: 1, borderColor: '#eee' },
  searchBtn: { backgroundColor: '#34C759', borderRadius: 10, padding: 14, alignItems: 'center', marginTop: 12 },
  searchBtnText: { color: '#fff', fontSize: 16, fontWeight: 'bold' },
});
```

- [ ] **Step 3: route-result.tsx (경로 상세) 작성**

```typescript
// app/route-result.tsx
import React from 'react';
import { View, Text, ScrollView, TouchableOpacity, StyleSheet } from 'react-native';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { PathResult } from '../lib/api';

export default function RouteResultScreen() {
  const { pathJson } = useLocalSearchParams<{ pathJson: string }>();
  const router = useRouter();
  const path: PathResult = JSON.parse(pathJson);

  return (
    <ScrollView style={styles.container}>
      <Text style={styles.header}>
        총 {path.info.totalTime}분 · {path.info.payment.toLocaleString()}원
      </Text>
      {path.subPaths.map((sub, i) => (
        <View key={i} style={styles.segment}>
          <Text style={styles.segType}>
            {sub.trafficType === 1 ? '🚇 지하철' : sub.trafficType === 2 ? '🚌 버스' : '🚶 도보'}
            {' '}({sub.sectionTime}분)
          </Text>
          <Text style={styles.segRoute}>
            {sub.lanes[0]?.name ?? sub.lanes[0]?.busNo ?? ''}{' '}
            {sub.startName} → {sub.endName}
          </Text>
          {sub.trafficType !== 3 && sub.stations[0]?.stationID && (
            <TouchableOpacity
              style={styles.arrivalBtn}
              onPress={() => {
                const sid = sub.stations[0].stationID;
                if (sub.trafficType === 1) router.push(`/arrivals/subway/${sid}`);
                else router.push(`/arrivals/bus/${sid}`);
              }}
            >
              <Text style={styles.arrivalBtnText}>도착정보 보기</Text>
            </TouchableOpacity>
          )}
        </View>
      ))}
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#f5f5f5', padding: 16, paddingTop: 60 },
  header: { fontSize: 18, fontWeight: 'bold', marginBottom: 16 },
  segment: { backgroundColor: '#fff', borderRadius: 10, padding: 14, marginBottom: 10 },
  segType: { fontWeight: 'bold', fontSize: 15 },
  segRoute: { color: '#555', marginTop: 4 },
  arrivalBtn: { marginTop: 10, backgroundColor: '#007AFF', borderRadius: 8, padding: 8, alignItems: 'center' },
  arrivalBtnText: { color: '#fff', fontWeight: 'bold' },
});
```

- [ ] **Step 4: Expo Go로 동작 확인**

```bash
npx expo start
```
Expo Go 앱에서 QR 스캔 → 경로검색 탭 → 목적지 입력 → 길찾기 → 결과 확인

- [ ] **Step 5: 커밋**

```bash
git add app/(tabs)/index.tsx app/route-result.tsx components/RouteCard.tsx
git commit -m "feat: add route search screen + route result screen"
```

---

## Task 9: 즐겨찾기 화면

**Files:**
- Modify: `app/(tabs)/favorites.tsx`

- [ ] **Step 1: 즐겨찾기 탭 작성**

```typescript
// app/(tabs)/favorites.tsx
import React, { useEffect, useState } from 'react';
import {
  View, Text, TouchableOpacity, FlatList,
  StyleSheet, Alert, Modal, TextInput,
} from 'react-native';
import { useRouter } from 'expo-router';
import { getFavorites, addFavorite, deleteFavorite, searchAddress, FavoriteItem } from '../../lib/api';

export default function FavoritesScreen() {
  const [favorites, setFavorites] = useState<FavoriteItem[]>([]);
  const [showModal, setShowModal] = useState(false);
  const [query, setQuery] = useState('');
  const [suggestions, setSuggestions] = useState<{ place_name: string; x: string; y: string }[]>([]);
  const [selectedType, setSelectedType] = useState<'HOME' | 'WORK'>('HOME');
  const router = useRouter();

  useEffect(() => { loadFavorites(); }, []);

  const loadFavorites = async () => {
    const res = await getFavorites();
    setFavorites(res.data);
  };

  const handleSearch = async () => {
    const docs = await searchAddress(query);
    setSuggestions(docs.slice(0, 5));
  };

  const handleAdd = async (doc: { place_name: string; x: string; y: string }) => {
    await addFavorite({
      type: selectedType,
      endPlace: doc.place_name,
      ex: parseFloat(doc.x),
      ey: parseFloat(doc.y),
    });
    setSuggestions([]);
    setShowModal(false);
    setQuery('');
    loadFavorites();
  };

  const handleDelete = (id: number) => {
    Alert.alert('삭제', '즐겨찾기를 삭제할까요?', [
      { text: '취소' },
      { text: '삭제', style: 'destructive', onPress: async () => {
        await deleteFavorite(id);
        loadFavorites();
      }},
    ]);
  };

  const handleNavigate = (fav: FavoriteItem) => {
    router.push({
      pathname: '/',
      params: { destName: fav.endPlace, destX: fav.ex, destY: fav.ey },
    });
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>즐겨찾기</Text>
      <TouchableOpacity style={styles.addBtn} onPress={() => setShowModal(true)}>
        <Text style={styles.addBtnText}>+ 추가</Text>
      </TouchableOpacity>

      <FlatList
        data={favorites}
        keyExtractor={(item) => String(item.id)}
        renderItem={({ item }) => (
          <View style={styles.item}>
            <TouchableOpacity style={{ flex: 1 }} onPress={() => handleNavigate(item)}>
              <Text style={styles.itemIcon}>{item.type === 'HOME' ? '🏠' : '🏢'}</Text>
              <Text style={styles.itemName}>{item.endPlace}</Text>
            </TouchableOpacity>
            <TouchableOpacity onPress={() => handleDelete(item.id)}>
              <Text style={styles.deleteBtn}>삭제</Text>
            </TouchableOpacity>
          </View>
        )}
        ListEmptyComponent={<Text style={styles.empty}>즐겨찾기가 없습니다</Text>}
      />

      <Modal visible={showModal} animationType="slide" transparent>
        <View style={styles.modal}>
          <Text style={styles.modalTitle}>즐겨찾기 추가</Text>
          <View style={styles.typeRow}>
            {(['HOME', 'WORK'] as const).map((t) => (
              <TouchableOpacity
                key={t}
                style={[styles.typeBtn, selectedType === t && styles.typeBtnActive]}
                onPress={() => setSelectedType(t)}
              >
                <Text style={selectedType === t ? { color: '#fff' } : {}}>
                  {t === 'HOME' ? '🏠 집' : '🏢 회사'}
                </Text>
              </TouchableOpacity>
            ))}
          </View>
          <View style={styles.inputRow}>
            <TextInput
              style={styles.input}
              placeholder="장소 검색"
              value={query}
              onChangeText={setQuery}
              onSubmitEditing={handleSearch}
            />
            <TouchableOpacity style={styles.btn} onPress={handleSearch}>
              <Text style={styles.btnText}>검색</Text>
            </TouchableOpacity>
          </View>
          {suggestions.map((s, i) => (
            <TouchableOpacity key={i} style={styles.suggestion} onPress={() => handleAdd(s)}>
              <Text>{s.place_name}</Text>
            </TouchableOpacity>
          ))}
          <TouchableOpacity style={styles.closeBtn} onPress={() => setShowModal(false)}>
            <Text>닫기</Text>
          </TouchableOpacity>
        </View>
      </Modal>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#f5f5f5', padding: 16, paddingTop: 60 },
  title: { fontSize: 24, fontWeight: 'bold', marginBottom: 16 },
  addBtn: { backgroundColor: '#007AFF', borderRadius: 10, padding: 12, alignItems: 'center', marginBottom: 16 },
  addBtnText: { color: '#fff', fontWeight: 'bold' },
  item: { flexDirection: 'row', backgroundColor: '#fff', borderRadius: 10, padding: 14, marginBottom: 8, alignItems: 'center' },
  itemIcon: { fontSize: 20 },
  itemName: { fontSize: 16, marginTop: 4 },
  deleteBtn: { color: '#FF3B30', fontWeight: 'bold' },
  empty: { textAlign: 'center', marginTop: 40, color: '#999' },
  modal: { flex: 1, backgroundColor: '#fff', padding: 24, paddingTop: 60 },
  modalTitle: { fontSize: 20, fontWeight: 'bold', marginBottom: 16 },
  typeRow: { flexDirection: 'row', gap: 8, marginBottom: 16 },
  typeBtn: { borderWidth: 1, borderColor: '#ddd', borderRadius: 8, padding: 10 },
  typeBtnActive: { backgroundColor: '#007AFF', borderColor: '#007AFF' },
  inputRow: { flexDirection: 'row', gap: 8, marginBottom: 8 },
  input: { flex: 1, borderWidth: 1, borderColor: '#ddd', borderRadius: 8, padding: 12, backgroundColor: '#fff' },
  btn: { backgroundColor: '#007AFF', borderRadius: 8, padding: 12, justifyContent: 'center' },
  btnText: { color: '#fff', fontWeight: 'bold' },
  suggestion: { padding: 12, borderBottomWidth: 1, borderColor: '#eee' },
  closeBtn: { marginTop: 16, alignItems: 'center' },
});
```

- [ ] **Step 2: Expo Go로 동작 확인**

즐겨찾기 탭 → 추가 → 장소 검색 → 선택 → 목록 확인 → 탭하면 경로검색 탭으로 이동

- [ ] **Step 3: 커밋**

```bash
git add app/(tabs)/favorites.tsx
git commit -m "feat: add favorites screen with add/delete/navigate"
```

---

## Task 10: 도착정보 화면

**Files:**
- Create: `app/arrivals/bus/[stopId].tsx`
- Create: `app/arrivals/subway/[stationId].tsx`
- Create: `components/ArrivalList.tsx`

- [ ] **Step 1: ArrivalList 공통 컴포넌트 생성**

```typescript
// components/ArrivalList.tsx
import React from 'react';
import { View, Text, StyleSheet, FlatList } from 'react-native';

interface ArrivalRow {
  key: string;
  label: string;
  detail: string;
  sub?: string;
}

interface Props {
  title: string;
  items: ArrivalRow[];
  emptyMessage?: string;
}

export function ArrivalList({ title, items, emptyMessage = '도착정보가 없습니다' }: Props) {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>{title}</Text>
      <FlatList
        data={items}
        keyExtractor={(item) => item.key}
        renderItem={({ item }) => (
          <View style={styles.row}>
            <View style={{ flex: 1 }}>
              <Text style={styles.label}>{item.label}</Text>
              {item.sub && <Text style={styles.sub}>{item.sub}</Text>}
            </View>
            <Text style={styles.detail}>{item.detail}</Text>
          </View>
        )}
        ListEmptyComponent={<Text style={styles.empty}>{emptyMessage}</Text>}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#f5f5f5', padding: 16, paddingTop: 60 },
  title: { fontSize: 20, fontWeight: 'bold', marginBottom: 16 },
  row: { flexDirection: 'row', backgroundColor: '#fff', borderRadius: 10, padding: 14, marginBottom: 8, alignItems: 'center' },
  label: { fontSize: 15, fontWeight: '600' },
  sub: { color: '#888', marginTop: 2, fontSize: 13 },
  detail: { fontSize: 15, fontWeight: 'bold', color: '#007AFF' },
  empty: { textAlign: 'center', marginTop: 40, color: '#999' },
});
```

- [ ] **Step 2: 버스 도착정보 화면 생성**

```typescript
// app/arrivals/bus/[stopId].tsx
import React, { useEffect, useState } from 'react';
import { ActivityIndicator } from 'react-native';
import { useLocalSearchParams } from 'expo-router';
import { getBusArrivals, BusArrivalItem } from '../../../lib/api';
import { ArrivalList } from '../../../components/ArrivalList';

export default function BusArrivalScreen() {
  const { stopId } = useLocalSearchParams<{ stopId: string }>();
  const [items, setItems] = useState<BusArrivalItem[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    getBusArrivals(stopId)
      .then((res) => setItems(res.data))
      .finally(() => setLoading(false));
  }, [stopId]);

  if (loading) return <ActivityIndicator style={{ flex: 1 }} />;

  const rows = items.flatMap((item) => {
    const result = [];
    if (item.arrivalMsg1) {
      result.push({
        key: `${item.routeId}-1`,
        label: item.routeId,
        sub: item.arrivalMsg1,
        detail: item.predictTime1 ? `${Math.round(item.predictTime1 / 60)}분` : '-',
      });
    }
    if (item.arrivalMsg2) {
      result.push({
        key: `${item.routeId}-2`,
        label: item.routeId,
        sub: item.arrivalMsg2,
        detail: item.predictTime2 ? `${Math.round(item.predictTime2 / 60)}분` : '-',
      });
    }
    return result;
  });

  return <ArrivalList title="버스 도착정보" items={rows} />;
}
```

- [ ] **Step 3: 지하철 도착정보 화면 생성**

```typescript
// app/arrivals/subway/[stationId].tsx
import React, { useEffect, useState } from 'react';
import { ActivityIndicator } from 'react-native';
import { useLocalSearchParams } from 'expo-router';
import { getSubwayArrivals, SubwayArrivalItem } from '../../../lib/api';
import { ArrivalList } from '../../../components/ArrivalList';

const directionLabel = (d: string) =>
  d === '상행' || d === '내선' ? '⬆ ' + d : '⬇ ' + d;

const secondsToLabel = (sec: number) => {
  if (sec <= 0) return '곧 도착';
  if (sec < 60) return `${sec}초`;
  return `${Math.round(sec / 60)}분`;
};

export default function SubwayArrivalScreen() {
  const { stationId } = useLocalSearchParams<{ stationId: string }>();
  const [items, setItems] = useState<SubwayArrivalItem[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    getSubwayArrivals(stationId)
      .then((res) => setItems(res.data))
      .finally(() => setLoading(false));
  }, [stationId]);

  if (loading) return <ActivityIndicator style={{ flex: 1 }} />;

  const rows = items.map((item, i) => ({
    key: String(i),
    label: `${directionLabel(item.direction)} ${item.destinationName ?? ''}`,
    sub: item.currentMessage,
    detail: item.arrivalSeconds != null ? secondsToLabel(item.arrivalSeconds) : '-',
  }));

  return <ArrivalList title="지하철 도착정보" items={rows} />;
}
```

- [ ] **Step 4: Expo Go로 동작 확인**

경로검색 → 경로 상세 → '도착정보 보기' 탭 → 도착정보 화면 확인

- [ ] **Step 5: 커밋**

```bash
git add app/arrivals/ components/ArrivalList.tsx
git commit -m "feat: add bus/subway arrival screens"
```

---

## Task 11: 웹 Export + Vercel 배포

- [ ] **Step 1: app.json에 웹 설정 추가**

```json
// app.json — expo 섹션 안에 추가
{
  "expo": {
    "web": {
      "bundler": "metro",
      "output": "static",
      "favicon": "./assets/images/favicon.png"
    }
  }
}
```

- [ ] **Step 2: 웹 빌드**

```bash
npx expo export --platform web
```
Expected: `dist/` 폴더 생성

- [ ] **Step 3: Vercel CLI로 배포**

```bash
npm install -g vercel
vercel --cwd dist
```

프롬프트:
- Set up and deploy → `y`
- Project name → `nextroute-app`
- 배포 완료 → `https://nextroute-app-xxx.vercel.app` URL 확인

- [ ] **Step 4: 환경변수 Vercel에 등록**

```bash
vercel env add EXPO_PUBLIC_API_BASE_URL production
# 값 입력: https://your-vps-domain.com

vercel env add EXPO_PUBLIC_KAKAO_REST_API_KEY production
# 값 입력: your_kakao_key
```

- [ ] **Step 5: 환경변수 적용 후 재배포**

```bash
npx expo export --platform web
vercel --cwd dist --prod
```

- [ ] **Step 6: 배포된 URL 동작 확인**

브라우저에서 배포 URL 접속 → 경로검색 → 즐겨찾기 확인

- [ ] **Step 7: CORS 업데이트 (Spring Boot)**

`env/api.env`에서 Vercel URL 업데이트 후 VPS 재배포:

```bash
# VPS에서
cd /path/to/nextroute
docker compose pull && docker compose up -d nextroute
```

- [ ] **Step 8: 커밋**

```bash
git add app.json
git commit -m "chore: configure expo web export for Vercel deployment"
```

---

## 자체 검토

**스펙 커버리지:**
- [x] 경로 검색 — Task 8
- [x] 즐겨찾기 저장/조회/삭제 — Task 4, 5, 9
- [x] 실시간 버스 도착정보 — Task 2, 10
- [x] 실시간 지하철 도착정보 — Task 3, 10
- [x] deviceId 기반 User (Spring Security 추후 대비 deviceId 컬럼 보존) — Task 1
- [x] expo export --platform web → Vercel 배포 — Task 11

**잠재적 이슈:**
- ODSay `stationID`와 서울 API `stopId`/`stationId`가 다를 수 있음 → 도착정보가 안 뜰 경우 `stationName` 기반 검색 API 추가 필요 (MVP 이후)
- Kakao REST API 키 필요 (무료 발급, `developers.kakao.com`)
